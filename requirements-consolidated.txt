# Core dependencies for TailOpsMCP project (Python 3.12+)
# Install with: pip install -r requirements.txt

# Core MCP framework
fastmcp>=1.0.0
mcp>=1.0.0

# System interaction
psutil>=5.9.0
paramiko>=2.9.0

# Container management
docker>=7.0.0

# Data validation and parsing
pydantic>=2.0.0
pyyaml>=6.0.1

# Cryptographic and security
cryptography>=42.0.0

# Network and HTTP
aiohttp>=3.9.0
dnspython>=2.4.0
requests>=2.31.0
python-multipart>=0.0.6
uvicorn>=0.25.0

# Development dependencies (merged for consistency)
black>=23.0.0
flake8>=6.0.0
mypy>=1.7.0
pre-commit>=3.5.0
pytest>=7.4.0
pytest-asyncio>=0.21.0
requests-mock>=1.10.0
bandit[toml]>=1.7.10
isort>=5.12.0
ruff>=0.6.0
safety>=3.0.0
shellcheck-py>=0.10.0.0
types-psutil>=5.9.0